SOURCE_URL="http://www.tinc-vpn.org/packages/tinc-${VERSION}.tar.gz"
DEPENDENCIES="openssl,zlib,lzo"

PRE_CONFIGURE_COMMAND=
CONFIGURE_TOOL=
CONFIGURE_ARGS="--prefix=$PREFIX"
POST_CONFIGURE_COMMAND=

PRE_MAKE_COMMAND=
MAKE_TOOL=
MAKE_ARGS="-j install DESTDIR=$DESTDIR"
POST_MAKE_COMMAND=

