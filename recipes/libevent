SOURCE_URL="http://downloads.sourceforge.net/project/levent/release-${VERSION}-stable/libevent-${VERSION}-stable.tar.gz"
DEPENDENCIES="zlib,openssl"

PRE_CONFIGURE_COMMAND=
CONFIGURE_TOOL=
CONFIGURE_ARGS="--prefix=$PREFIX"
POST_CONFIGURE_COMMAND=

PRE_MAKE_COMMAND=
MAKE_TOOL=
MAKE_ARGS="-j4"
POST_MAKE_COMMAND="make install DESTDIR=$DESTDIR"
