SOURCE_URL="http://www.lua.org/ftp/lua-${VERSION}.tar.gz"
DEPENDENCIES=

PRE_CONFIGURE_COMMAND=
CONFIGURE_TOOL="true"
CONFIGURE_ARGS=
POST_CONFIGURE_COMMAND=

PRE_MAKE_COMMAND=
MAKE_TOOL=
MAKE_ARGS="linux install INSTALL_TOP=$DESTDIR"
POST_MAKE_COMMAND=

