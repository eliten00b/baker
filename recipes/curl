SOURCE_URL="https://github.com/bagder/curl/releases/download/curl-${VERSION//./_}/curl-${VERSION}.tar.gz"
DEPENDENCIES="openssl"

PRE_CONFIGURE_COMMAND=
CONFIGURE_TOOL=
CONFIGURE_ARGS='--prefix=$PREFIX --with-ssl=/usr/local'
POST_CONFIGURE_COMMAND=

PRE_MAKE_COMMAND=
MAKE_TOOL=
MAKE_ARGS="-j4 install DESTDIR=$DESTDIR"
POST_MAKE_COMMAND=
